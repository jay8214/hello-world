version: 0.1

phases:
  build:
    commands:
      - echo Build started on `date`
  post_build:
    commands:
      - echo Build completed on `date`
artifacts:
    files:
      - index.php
      - appspec.yml
discard-paths: yes
